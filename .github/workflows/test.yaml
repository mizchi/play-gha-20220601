name: Reusable workflow example

on:
  pull_request:
    branches:
      - main

jobs:
  setup:
    name: setup
    uses: {owner}/{repo}/{path}/setup.yaml@{ref}
    secrets:
      token: ${{ secrets.TOKEN }}

  job2:
    runs-on: ubuntu-latest
    needs: setup
    steps:
      - run: ls .
